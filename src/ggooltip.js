"use strict";

(function (simplelib) {
	if (!simplelib) return {}
	const sl = new simplelib();

	const gg = function (conf) {

		let config = require('./config')(sl, conf);

		return 
	}

	sl.util.proto(simplelib, 'gg', gg);
} (window.simplelib));
